# Velvet Cartographers' Voyage

> **Why.** Turn-based lantern-fuel survival where you chart constellations across hex seas before ink or light runs dry.

## Features

- Pure `(state, action) → newState + events` engine per `stateless-turn-combat-engine` and `immutable-action-event-log`.
- Soft/hard pity star-discovery rolls adapted from `gacha-soft-hard-pity`; status effects per `status-effect-enum-system`.
- Risk circuit-breaker, consecutive-freeze guard, baseline drift all tuned via `layered-risk-gates`, `frozen-detection-consecutive-count`, `baseline-historical-comparison-threshold`.

## File structure

```
velvet-cartographers-voyage/
  index.html    — shell, markup, inline SVG where used
  style.css     — dark-theme styling and animations
  app.js        — interactions, simulated data, render loop
  manifest.json — hub metadata
```

## Usage

```bash
# any static file server works
python -m http.server 8080
# or open index.html directly in a browser
```

## Stack

`html` · `css` · `vanilla-js` — zero dependencies, 239 lines

## Provenance

- Generated by auto-hub-loop cycle 1 on 2026-04-17
- Theme keyword: `Velvet cartographers chart forgotten constellations across drifting paper seas where inkwell whales surface beneath lantern-lit skies`
- Source working copy: `18-velvet-cartographers-voyage`
