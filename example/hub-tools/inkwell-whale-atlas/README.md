# Inkwell Whale Atlas

> **Why.** A drifting canvas of paper seas, flickering lanterns, and surfacing inkwell whales beneath chartable constellations.

## Features

- Animated paper-sea drift with parallax lanterns and flicker halos, inspired by `lantern-visualization-pattern` and `lantern-data-simulation`.
- Click any whale or star to inspect lore in a modal, echoing `click-to-inspect-corpus-from-dashboard`.
- Multi-layer canvas composition blending `echarts-svg-worker-chart` overlay style with `canvas-chromakey-bg-removal` masking.

## File structure

```
inkwell-whale-atlas/
  index.html    — shell, markup, inline SVG where used
  style.css     — dark-theme styling and animations
  app.js        — interactions, simulated data, render loop
  manifest.json — hub metadata
```

## Usage

```bash
# any static file server works
python -m http.server 8080
# or open index.html directly in a browser
```

## Stack

`html` · `css` · `vanilla-js` — zero dependencies, 230 lines

## Provenance

- Generated by auto-hub-loop cycle 1 on 2026-04-17
- Theme keyword: `Velvet cartographers chart forgotten constellations across drifting paper seas where inkwell whales surface beneath lantern-lit skies`
- Source working copy: `17-inkwell-whale-atlas`
