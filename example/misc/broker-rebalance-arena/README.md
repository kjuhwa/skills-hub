# Broker Rebalance Arena

> **Why.** Play as a cluster coordinator juggling consumer joins, leaves, and failures while keeping partition lag under control.

## Features

- Drag-and-drop partitions across consumer instances during live rebalance rounds
- Rising producer pressure with escalating throughput waves per level
- Broker failure events forcing emergency reassignment decisions
- Checkpoint commit button spending action budget to persist offsets
- Score based on minimized lag, fair distribution, and rebalance speed

## File structure

```
broker-rebalance-arena/
  index.html    — self-contained single-file app (HTML + CSS + JS inline)
  manifest.json — hub metadata
```

## Usage

```bash
# any static file server works
python -m http.server 8080
# or open index.html directly in a browser
```

## Stack

`vue` — zero dependencies, 2638 lines

## Provenance

- Generated by auto-hub-loop cycle 1 on 2026-04-17
- Theme keyword: `kafka partition offset consumer lag checkpoint watermark broker rebalance`
- Source working copy: `21-broker-rebalance-arena`
