# Offset Oracle — Compaction Forecast Terminal

> **Why.** A terminal-style calculator that simulates offset-index sizing, retention spend, and compaction ROI across replicas.

## Features

- d3 force-directed segment graph linking snapshots to segments via `distributed-tracing-visualization-pattern`
- Offset index scale bars using `d3.scaleLinear` inspired by `time-series-db-visualization-pattern` and `materialized-view-visualization-pattern`
- Retention cost calculator echoing `baseline-historical-comparison-threshold` and `divide-by-zero-rate-guard`
- Rebalance forecast view from `database-sharding-visualization-pattern`, `load-balancer-visualization-pattern`, `rebalance-partition-ownership-swimlane`
- Snapshot schedule tuner referencing `tiered-rebalance-schedule` and `calendar-cron-vs-spring-cron-migration`
- Tombstone DLQ pane referencing `dead-letter-queue-visualization-pattern` and `dead-letter-queue-data-simulation`
- Compaction ROI histogram via `log-aggregation-visualization-pattern` and `log-aggregation-data-simulation`
- Consistent-hashing scenario from `consistent-hashing-visualization-pattern` and `consistent-hashing-data-simulation`
- Budget simulator drawing on `thread-pool-queue-backpressure`, `backpressure-visualization-pattern`, `backpressure-data-simulation`
- Raft-ish leader ledger from `raft-consensus-visualization-pattern` and `raft-consensus-data-simulation`

## File structure

```
offset-oracle-terminal/
  index.html    — shell, markup, inline SVG where used
  style.css     — dark-theme styling and animations
  app.js        — interactions, simulated data, render loop
  manifest.json — hub metadata
```

## Usage

```bash
# any static file server works
python -m http.server 8080
# or open index.html directly in a browser
```

## Stack

`d3` — zero dependencies, 615 lines

## Provenance

- Generated by auto-hub-loop cycle 8 on 2026-04-17
- Theme keyword: `write-ahead log segment compaction tombstone snapshot offset index`
- Source working copy: `40-offset-oracle-terminal`
