# Watermark Tide Atlas

> **Why.** Explore stream windowing and watermark progression as an ocean of event tides with checkpoint barriers drifting through partitions.

## Features

- Multi-partition event-time canvas with animated watermark line, late-event shading, and session/tumbling/sliding window overlays inspired by `availability-ttl-punctuate-processor`, `lag-watermark-dual-axis-timeline`, `time-series-db-visualization-pattern`
- React-hook driven simulator generating bursty producers, backpressure hints, and rebalance events — drawing from `kafka-batch-consumer-partition-tuning`, `backpressure-visualization-pattern`, `rebalance-partition-ownership-swimlane`, `kafka-consumer-semaphore-chunking`
- Inspector panel with vector-clock-style causality grid, checkpoint barrier replay scrubber, and changelog topic trace modeled on `replica-timeline-swimlane-with-causal-arrows`, `vector-clock-concurrency-matrix`, `event-sourcing-visualization-pattern`, `transaction-trace-pack-serialization-with-step-hierarchy`

## File structure

```
watermark-tide-atlas/
  index.html    — shell, markup, inline SVG where used
  style.css     — dark-theme styling and animations
  app.js        — interactions, simulated data, render loop
  manifest.json — hub metadata
```

## Usage

```bash
# any static file server works
python -m http.server 8080
# or open index.html directly in a browser
```

## Stack

`react` — zero dependencies, 875 lines

## Provenance

- Generated by auto-hub-loop cycle 3 on 2026-04-17
- Theme keyword: `stream processor windowing watermark checkpoint barrier state backend changelog topic`
- Source working copy: `26-watermark-tide-atlas`
