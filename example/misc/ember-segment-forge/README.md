# Ember Segment Forge — Compaction Simulator

> **Why.** Run a hand-tuned compaction simulation where tombstones smolder, snapshots flare, and offsets advance like embers.

## Features

- Play/pause compaction ticker driven by `finite-state-machine-data-simulation` and `finite-state-machine-visualization-pattern`
- Adaptive compaction policy based on `adaptive-strategy-hot-swap` and `layered-risk-gates`
- Retry/back-off for failed merges via `retry-strategy-data-simulation` and `retry-strategy-visualization-pattern`
- Tombstone pressure budget pulled from `rate-limiter-data-simulation` and `rate-limiter-visualization-pattern`
- Segment lane SVG render inspired by `outbox-pattern-visualization-pattern` and `saga-pattern-visualization-pattern`
- Lease/epoch fencing toggle drawing on `lease-epoch-fencing-token-monotonic-guard` and `distributed-lock-mongodb`
- Debounce compaction bursts via `kafka-debounce-event-coalescing` and `thread-pool-queue-backpressure`
- Jittered TTL dials referencing `cache-variance-ttl-jitter`, `availability-ttl-punctuate-processor`, and `mongo-ttl-with-aggregation-delta`
- Consumer lag swimlane echoing `pub-sub-visualization-pattern` and `connection-pool-visualization-pattern`
- Snapshot canon mechanic via `gacha-soft-hard-pity` and `phase-window-timing-grade-with-pity`

## File structure

```
ember-segment-forge/
  index.html    — shell, markup, inline SVG where used
  style.css     — dark-theme styling and animations
  app.js        — interactions, simulated data, render loop
  manifest.json — hub metadata
```

## Usage

```bash
# any static file server works
python -m http.server 8080
# or open index.html directly in a browser
```

## Stack

`vue3` — zero dependencies, 654 lines

## Provenance

- Generated by auto-hub-loop cycle 8 on 2026-04-17
- Theme keyword: `write-ahead log segment compaction tombstone snapshot offset index`
- Source working copy: `39-ember-segment-forge`
