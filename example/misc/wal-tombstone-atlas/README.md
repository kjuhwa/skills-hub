# WAL Tombstone Atlas — Segment Compaction Explorer

> **Why.** Navigate write-ahead log segments, tombstone lifecycles, and snapshot offset indexes across a simulated replicated store.

## Features

- Interactive segment timeline with compaction playback inspired by `changelog-compaction-tombstone-retention` and `event-sourcing-visualization-pattern`
- Offset index scrubber with watermark overlays drawing on `watermark-aligned-window-emitter` and `lag-watermark-dual-axis-timeline`
- Tombstone retention heatmap wired to `cdc-visualization-pattern`, `audit-change-data-capture-pattern`, and `materialized-view-visualization-pattern`
- Replica causal arrows via `replica-timeline-swimlane-with-causal-arrows` and `vector-clock-concurrency-matrix`
- Rebalance diff inspector leaning on `rebalance-partition-ownership-swimlane` and `minimal-key-movement-rebalance-diff`
- Snapshot atlas cards referencing `event-sourcing-data-simulation`, `cqrs-visualization-pattern`, and `command-query-visualization-pattern`
- Bloom-style tombstone filter preview from `bloom-filter-visualization-pattern` and `bloom-filter-data-simulation`
- Compaction fencing inspector via `lease-epoch-fencing-token-monotonic-guard` and `hybrid-logical-clock-merge`
- Segment byte-budget tips from `compact-binary-wire-protocol-with-variable-length-encoding` and `byte-aware-sms-truncation-with-ellipsis`
- Retention jitter overlay from `cache-variance-ttl-jitter` and `mongo-ttl-with-aggregation-delta`

## File structure

```
wal-tombstone-atlas/
  index.html    — shell, markup, inline SVG where used
  style.css     — dark-theme styling and animations
  app.js        — interactions, simulated data, render loop
  manifest.json — hub metadata
```

## Usage

```bash
# any static file server works
python -m http.server 8080
# or open index.html directly in a browser
```

## Stack

`react` — zero dependencies, 626 lines

## Provenance

- Generated by auto-hub-loop cycle 8 on 2026-04-17
- Theme keyword: `write-ahead log segment compaction tombstone snapshot offset index`
- Source working copy: `38-wal-tombstone-atlas`
