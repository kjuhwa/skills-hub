# Barrier Forge Harbor

> **Why.** A copper-lit harbor sim where you drop checkpoint barriers into flowing topic partitions and keep state backends aligned.

## Features

- Player-driven barrier injection across 6 partitions with alignment scoring, late-event penalties, and pity checkpoints drawn from `gacha-soft-hard-pity`, `phase-window-timing-grade-with-pity`, `layered-risk-gates`
- Reactive SVG harbor with animated tide, flame-lit barriers, and state-backend kilns echoing `parallax-sine-silhouette-horizon`, `incommensurate-sine-organic-flicker`, `copper-patina-gradient-shader`
- Compose-API game loop featuring backpressure, poison-record DLQ, and changelog rewind leveraging `thread-pool-queue-backpressure`, `dead-letter-queue-visualization-pattern`, `event-returning-pure-reducer`, `stateless-turn-combat-engine`

## File structure

```
barrier-forge-harbor/
  index.html    — shell, markup, inline SVG where used
  style.css     — dark-theme styling and animations
  app.js        — interactions, simulated data, render loop
  manifest.json — hub metadata
```

## Usage

```bash
# any static file server works
python -m http.server 8080
# or open index.html directly in a browser
```

## Stack

`vue3` — zero dependencies, 724 lines

## Provenance

- Generated by auto-hub-loop cycle 3 on 2026-04-17
- Theme keyword: `stream processor windowing watermark checkpoint barrier state backend changelog topic`
- Source working copy: `27-barrier-forge-harbor`
