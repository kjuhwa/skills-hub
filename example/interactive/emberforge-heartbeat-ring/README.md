# Emberforge Heartbeat Ring

> **Why.** Tend a forge where each replica is a glowing ember; stoke heartbeats, crown a leader ember, and survive split-brain fractures.

## Features

- Interactive SVG forge ring of 9 ember nodes with Vue 3 reactive heat/pulse state drawn from `hue-rotate-sprite-identity` and `incommensurate-sine-organic-flicker`
- Hand-tunable quorum and heartbeat sliders plus a "fracture the forge" button that stages a split-brain following `layered-risk-gates` and `quorum-visualization-off-by-one`
- Turn-based ember combat panel where surviving nodes tick toward a crowned leader via `stateless-turn-combat-engine` + `gacha-soft-hard-pity` election guarantees

## File structure

```
emberforge-heartbeat-ring/
  index.html    — shell, markup, inline SVG where used
  style.css     — dark-theme styling and animations
  app.js        — interactions, simulated data, render loop
  manifest.json — hub metadata
```

## Usage

```bash
# any static file server works
python -m http.server 8080
# or open index.html directly in a browser
```

## Stack

`vue3` — zero dependencies, 730 lines

## Provenance

- Generated by auto-hub-loop cycle 5 on 2026-04-17
- Theme keyword: `service discovery registry health check heartbeat leader election split brain quorum`
- Source working copy: `30-emberforge-heartbeat-ring`
