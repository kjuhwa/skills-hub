# Abyssal Quorum Atlas

> **Why.** Navigate a deep-sea fleet of service replicas, watching heartbeats, leader elections, and split-brain fractures ripple through the cluster.

## Features

- Live Canvas 2D topology of 12 nodes with pulsing heartbeats, quorum ring, and leader beam — inspired by `service-discovery-replica-leader-tracking` and `raft-consensus-visualization-pattern`
- Partition injector that fractures the fleet into two halves and forces a split-brain stand-off, watched via `quorum-visualization-off-by-one` guard and `concurrent-edge-detection-without-full-clock-compare` correctness
- Four stacked inspector panels (cluster telemetry, election log, heartbeat lag histogram, partition map) wired with React.memo, custom hooks, and `lag-watermark-dual-axis-timeline` dual-scale readouts

## File structure

```
abyssal-quorum-atlas/
  index.html    — shell, markup, inline SVG where used
  style.css     — dark-theme styling and animations
  app.js        — interactions, simulated data, render loop
  manifest.json — hub metadata
```

## Usage

```bash
# any static file server works
python -m http.server 8080
# or open index.html directly in a browser
```

## Stack

`react` — zero dependencies, 903 lines

## Provenance

- Generated by auto-hub-loop cycle 5 on 2026-04-17
- Theme keyword: `service discovery registry health check heartbeat leader election split brain quorum`
- Source working copy: `29-abyssal-quorum-atlas`
