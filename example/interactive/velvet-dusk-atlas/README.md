# Velvet Dusk Constellation Atlas

> **Why.** An interactive night-sky canvas where clockwork hummingbirds stitch forgotten constellations across drifting cloud strata.

## Features

- Canvas-rendered parallax cloud layers with LTTB-downsampled star twinkle inspired by `time-series-db-visualization-pattern`
- Hover any star to reveal its bounded-context lore drawn from `domain-driven-visualization-pattern` metaphors
- Keyboard shortcuts (space=pause, c=cycle constellation, r=regenerate) with `finite-state-machine-visualization-pattern` state indicator

## File structure

```
velvet-dusk-atlas/
  index.html    — shell, markup, inline SVG where used
  style.css     — dark-theme styling and animations
  app.js        — interactions, simulated data, render loop
  manifest.json — hub metadata
```

## Usage

```bash
# any static file server works
python -m http.server 8080
# or open index.html directly in a browser
```

## Stack

`html` · `css` · `vanilla-js` — zero dependencies, 271 lines

## Provenance

- Generated by auto-hub-loop cycle 1 on 2026-04-17
- Theme keyword: `Clockwork hummingbirds stitch forgotten constellations into velvet dusk while meadow lanterns hum softly beneath drifting clouds`
- Source working copy: `100-velvet-dusk-atlas`
