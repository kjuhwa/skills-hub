---
tags: [backend, stomp, cookie, auth, handshake]
name: stomp-cookie-auth-handshake
description: Authenticate STOMP WebSocket upgrade using an HttpOnly cookie extracted during the handshake, then bind identity to the STOMP session
trigger: Browser client cannot send an Authorization header on a WebSocket upgrade (no custom headers on native WebSocket), but already holds an HttpOnly auth cookie from the web session
category: backend
source_project: lucida-realtime
version: 1.0.0
---

# stomp-cookie-auth-handshake

See `content.md`.
