---
tags: [backend, feed, envelope, frame]
name: feed-envelope-frame
description: Uniform WebSocket payload wrapper with Type (ACK/SNAPSHOT/DELTA/ERROR), PayloadKind, and correlation requestId, including null-safe factory methods
trigger: Designing a real-time push API (WebSocket, SSE, or long-poll) that must support initial snapshots, incremental deltas, acks, and error frames on the same channel — and clients need to correlate frames to requests
category: backend
source_project: lucida-realtime
version: 1.0.0
---

# feed-envelope-frame

See `content.md`.
